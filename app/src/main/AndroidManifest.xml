<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:label="Event System"
        android:supportsRtl="true">
        <activity android:name=".UserProfileActivity" android:screenOrientation="fullSensor" />
        <activity android:name=".EditEventActivity" android:screenOrientation="fullSensor" />
        <activity android:name=".EventDetailsActivity" android:screenOrientation="fullSensor" />
        <activity android:name=".MainActivity" android:exported="true" android:screenOrientation="fullSensor">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
